{
  "name": "ardusim",
  "version": "1.0.0",
  "description": "ArduSim â€” Arduino Online Simulator with a Node.js backend (static serving + project sync)",
  "main": "server.js",
  "scripts": {
    "setup-compilers": "node setup-compilers.js",
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "keywords": [
    "arduino",
    "simulator",
    "circuit",
    "iot"
  ],
  "license": "MIT",
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "eslint": "^10.10.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.5.0",
    "vitest": "^3.1.1"
  }
}

